<source>
  @type http
  port 9880
  bind 0.0.0.0
  body_size_limit 32m
  keepalive_timeout 10s
</source>

<filter **>
  @type record_transformer
  <record>
    host_param "#{Socket.gethostname}"
    tag ${tag}
  </record>
</filter>

<match **>
  @type file
  path /output/http.log
</match>